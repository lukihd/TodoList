doctype html
html
    head
        include ./../prefabs/head.pug
    body
        include ./../prefabs/header.pug

        table
            tr
                th firstname
                th lastname
                th username
                th password
                th email
                th created_at
                th updated_at
            
                - var values = Users
                if (values.length > 1)
                    each users in values
                        tr
                            each val, index in users
                                td= val 
                else
                    tr
                        each user in values
                            td= user